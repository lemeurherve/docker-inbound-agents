@Library('pipeline-library@pull/372/head') _

def principalBranch = 'main'

parallel(
  failFast: false,
  'maven-jdk8-nanoserver': {
     buildDockerAndPublishImage('inbound-agent-maven-nanoserver:jdk8', [
      dockerfile: 'maven/jdk8/Dockerfile.nanoserver',
      agentLabels: 'windows',
      useContainer: false,
    ])
  },
)
